# Elastic Integration
Sensor output uses the canonical AgentGuard contract (`event.module=agentguard`, `event.dataset=agentguard.findings`, `agentguard.*`). The Elastic backend is `integrations/elastic-agentguard`.
