# Elastic Integration
Sensor output uses the canonical AgentGuard contract (`event.module=agentguard`, `event.dataset=agentguard.findings`, `agentguard.*`). The Elastic backend remains `elastic-integration-ai-sentinel`; any `ai_sentinel.*` fields are legacy compatibility mirrors required by that package.
