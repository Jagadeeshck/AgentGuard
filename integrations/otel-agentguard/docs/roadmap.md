# Roadmap

- Add adapter SDK helpers for API gateways, browser metadata extensions, MCP runtimes, and local LLM sensors.
- Publish backend mapping examples for OpenSearch, Splunk, and Datadog.
- Add policy packs for model approval, provider approval, cost budgets, privacy posture, and MCP risk.
- Add validation CI for JSON, YAML, NDJSON, and naming guardrails.
- Add optional enterprise audit adapters when vendors provide approved export APIs.
